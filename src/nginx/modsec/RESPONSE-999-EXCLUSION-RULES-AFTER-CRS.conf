# =========================================================================
# Exclusions ChessGuard — chargées APRÈS les règles CRS
# =========================================================================

# [Corrige 920350] On accède au service par une adresse IP :
#   - le healthcheck Docker tape sur https://127.0.0.1
#   - l'évaluation 42 se fait souvent via l'IP de la VM/VPS
# Un "Host header numérique" est donc légitime chez nous → on retire la règle.
SecRuleRemoveById 920350