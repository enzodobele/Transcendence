# =========================================================================
# Exclusions ChessGuard — chargées AVANT les règles CRS
# =========================================================================

# [Corrige 911100] Autoriser PATCH : ton front édite le profil via
# PATCH /api/auth/profile. On ajoute PATCH à la liste des méthodes permises.
# (On reste minimal/hardened : on n'autorise QUE ce que l'app utilise.)
SecAction \
    "id:1001,\
    phase:1,\
    nolog,\
    pass,\
    t:none,\
    setvar:'tx.allowed_methods=GET HEAD POST OPTIONS PATCH'"